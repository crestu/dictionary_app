
<h1>Lists</h1>

 <% @lists.each do |list| %>
    <div class = "list-name">
     <strong> <%= link_to list.list_name, list_path(list) %> </strong> <em></em><br>
    </div>

         <%= link_to "Delete", list_path(list), data: {
        turbo_method: :delete,
        turbo_confirm: "Are you sure?"

        
    } %>
      <%= link_to "Edit", edit_list_path(list), method: :post, data: {} %>
  <% end %>


<br>
<br>
<br>
<%= link_to "New List", new_list_path, method: :post, data: {} %>











