<h1><%= @list.list_name %></h1>

<% @words.each do |word| %>
  <ul><strong><%= word.word %></strong>: <em><%= word.definition %></em><br></ul>
    <%= link_to "Delete", word_path(word), data: {
        turbo_method: :delete,
        turbo_confirm: "Are you sure?"
    } %>
    <br>
<% end %>

<br>
<br>
<%= link_to 'Lists', lists_path %>

<%= link_to 'Search', search_path %>


